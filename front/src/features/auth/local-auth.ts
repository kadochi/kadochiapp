/**
 * Public demo credentials for local development only.
 *
 * They are intentionally not secrets. The server refuses the local auth mode
 * when the application is running in production.
 */
export const LOCAL_AUTH_PHONE = "09121234567";
export const LOCAL_AUTH_OTP = "1234";
export const LOCAL_AUTH_OTP_LENGTH = LOCAL_AUTH_OTP.length;

